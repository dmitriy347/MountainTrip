name: Database Backup to S3

on:
  schedule:
    # Каждый день в 3:00 UTC (6:00 по Екатеринбургу)
    - cron: '0 3 * * *'
  workflow_dispatch:  # Можно запустить вручную

jobs:
  backup:
    name: 🗄️ Backup PostgreSQL to S3
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔑 Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/github_actions
          chmod 600 ~/.ssh/github_actions
          # Добавляем -t rsa для IP-адресов
          ssh-keyscan -t rsa -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: 🗄️ Create database backup
        run: |
          BACKUP_FILE="db_backup_$(date +%Y-%m-%d_%H-%M-%S).sql.gz"
          
          ssh -i ~/.ssh/github_actions ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
            "cd /opt/skitrip && docker exec skitrip_db pg_dump -U postgres skitrip_db | gzip" > $BACKUP_FILE
          
          echo "BACKUP_FILE=$BACKUP_FILE" >> $GITHUB_ENV
          ls -lh $BACKUP_FILE

      - name: ☁️ Upload to Yandex Object Storage
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.YC_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.YC_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ru-central1
        run: |
          # AWS CLI уже установлен в GitHub Actions, просто используем его
          aws s3 cp $BACKUP_FILE s3://skitrip-backups/database/ \
            --endpoint-url=https://storage.yandexcloud.net
          
          echo "✅ Backup uploaded to Yandex Cloud!"

      - name: 🧹 Cleanup old backups (keep last 7 days)
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.YC_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.YC_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ru-central1
        run: |
          # Получаем список файлов
          CUTOFF_DATE=$(date -d '7 days ago' +%Y-%m-%d)
          
          aws s3 ls s3://skitrip-backups/database/ \
            --endpoint-url=https://storage.yandexcloud.net | while read -r line; do
            
            FILE_DATE=$(echo $line | awk '{print $1}')
            FILE_NAME=$(echo $line | awk '{print $4}')
            
            if [[ "$FILE_DATE" < "$CUTOFF_DATE" ]] && [[ -n "$FILE_NAME" ]]; then
              echo "Deleting old backup: $FILE_NAME"
              aws s3 rm s3://skitrip-backups/database/$FILE_NAME \
                --endpoint-url=https://storage.yandexcloud.net
            fi
          done
          
          echo "✅ Old backups cleaned up!"

      - name: 📊 Backup summary
        run: |
          echo "### 🎉 Backup completed successfully!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**File:** \`$BACKUP_FILE\`" >> $GITHUB_STEP_SUMMARY
          echo "**Size:** $(ls -lh $BACKUP_FILE | awk '{print $5}')" >> $GITHUB_STEP_SUMMARY
          echo "**Location:** \`s3://skitrip-backups/database/\`" >> $GITHUB_STEP_SUMMARY
          echo "**Storage:** Yandex Object Storage" >> $GITHUB_STEP_SUMMARY